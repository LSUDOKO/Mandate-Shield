node_modules/
dist/
*.tsbuildinfo
.env
.env.local
# The SQLite ledger, wherever a workspace package writes it. Unanchored so
# it matches packages/server/data/ as well as a root-level data/.
**/data/*.db
**/data/*.db-journal
**/data/*.db-wal
**/data/*.db-shm
apps/dashboard/dist/
apps/storefront/dist/
coverage/
.DS_Store

# Unrelated W3C WebMCP spec clone that lives in this directory.
webmcp/
.vercel

# Generated serverless bundle (built by scripts/build-api.mjs).
api/index.mjs
api/package.json

# Playwright MCP scratch output.
.playwright-mcp/
